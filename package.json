{
  "name": "secret-ballot-pages",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "dev": "wrangler pages dev public --compatibility-date=2026-06-17",
    "deploy:pages": "wrangler pages deploy public --project-name=mrvote",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260606.0",
    "typescript": "^5.8.3",
    "vitest": "^2.1.9",
    "wrangler": "^4.21.1"
  }
}
